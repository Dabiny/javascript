 // if (isJump) { // 0보다 큰 수가있으면 넘어갈 수 있다. 
            //     console.log(test);
            //     test = [...test.splice(index, 1), ...test.splice(index + 1)];
            // }
            // else {
            //     return answer;
            // }